[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[tool.black]
line-length = 100

[tool.pylint.messages]
disable = [
]

[tool.test-stages]
stages = ["@check", "@tests"]
